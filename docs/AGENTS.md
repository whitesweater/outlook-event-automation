<!-- OMX:AGENTS-INIT:MANAGED -->
<!-- Parent: ../AGENTS.md -->
# docs

This AGENTS.md scopes guidance to `docs`. Parent AGENTS guidance still applies unless this file narrows it for this subtree.

## Bootstrap Guardrails
- This is a lightweight scaffold generated by `omx agents-init`.
- Refresh updates the layout summary below and preserves the manual notes block.
- Keep only directory-specific guidance here; do not duplicate the root orchestration brain.

## Current Layout

### Files
- `index.html`

### Subdirectories
- None

<!-- OMX:AGENTS-INIT:MANUAL:START -->
## Local Notes
- This subtree is the GitHub Pages site. Keep it static and dependency-free unless the user asks for a build pipeline.
- `index.html` uses GSAP from CDN for explanatory animation. Respect reduced-motion support and keep the first viewport useful even if JavaScript fails.
- Do not add secrets, live endpoint tokens, or server IP-specific operational details to the public page.
- After editing, inspect the page in a browser or at least run a lightweight HTML/link sanity check and summarize what was verified.
<!-- OMX:AGENTS-INIT:MANUAL:END -->
