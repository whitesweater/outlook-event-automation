<!-- OMX:AGENTS-INIT:MANAGED -->
<!-- Parent: ../AGENTS.md -->
# outlook_event_automation

This AGENTS.md scopes guidance to `outlook_event_automation`. Parent AGENTS guidance still applies unless this file narrows it for this subtree.

## Bootstrap Guardrails
- This is a lightweight scaffold generated by `omx agents-init`.
- Refresh updates the layout summary below and preserves the manual notes block.
- Keep only directory-specific guidance here; do not duplicate the root orchestration brain.

## Current Layout

### Files
- `.env.example`
- `config.example.json`
- `event_agent.py`
- `README.md`

### Subdirectories
- `data/`
- `deploy/`
- `docs/`
- `fixtures/`
- `integrations/`
- `scripts/`

<!-- OMX:AGENTS-INIT:MANUAL:START -->
## Local Notes
- `event_agent.py` is the operational core: CLI, service loop, Microsoft/Google API clients, SQLite audit, webhook notifications, and lightweight HTTP API all live there.
- Preserve the standard-library-only design. Use structured JSON and existing helper functions instead of ad hoc parsing or new packages.
- Keep production runtime files out of Git: `.env`, `config.local.json`, `data/*.json`, `data/*.sqlite3`, tokens, and logs.
- Microsoft delegated scopes for Outlook calendar reads need `Calendars.Read` or `Calendars.ReadWrite`; mail scanning needs `Mail.Read`.
- `Daily Event Alert` is intentionally ignored. Cancellation, recall, multi-event summaries, missing times, and low-confidence extraction should stay review-oriented rather than auto-created.
- Calendar event bodies should retain source traceability: original subject, sender, received time, source link when available, and original mail body when configured.
- Manual calendar writes through the HTTP API must remain gated by `api.allow_write_actions=true` and request-body `confirmed=true`.
- For API, deployment, or integration changes, update `README.md` and relevant `integrations/*.md` in the same change. Hermes-facing behavior must be documented in `integrations/hermes.md`, and user-visible capability changes should also be reflected in `../docs/index.html`.
- Minimum verification for code edits: `python3 -m py_compile event_agent.py`. For calendar/API work, also test the relevant CLI/API path when credentials are available.
<!-- OMX:AGENTS-INIT:MANUAL:END -->
